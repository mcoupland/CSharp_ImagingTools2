using System;

namespace ImagingTools
{
    public enum Orientation { Portrait, Landscape };
    public enum ResizeType { Reduction, Enlargement };
    public enum ResizeFormat { Crop, Scale };
    public enum DimensionType { Width, Height };
}
